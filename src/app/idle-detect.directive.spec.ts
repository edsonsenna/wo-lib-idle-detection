import { IdleDetectDirective } from './idle-detect.directive';

describe('IdleDetectDirective', () => {
  it('should create an instance', () => {
    const directive = new IdleDetectDirective();
    expect(directive).toBeTruthy();
  });
});
